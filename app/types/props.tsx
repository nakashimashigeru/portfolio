export type FooterProps = {
  isLoading: boolean;
};

export type HeaderProps = {
  title: string;
  isLoading: boolean;
};

export type ImageProps = {
  url: string;
  alt: string;
  width: number;
  height: number;
};
